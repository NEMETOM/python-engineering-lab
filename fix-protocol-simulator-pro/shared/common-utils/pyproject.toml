[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "common-utils"
version = "0.1.0"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/common_utils"]
