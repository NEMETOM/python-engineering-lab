route:
  receiver: default
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  # Placeholder receiver: no notification integration configured.
  # Fired alerts are visible at http://localhost:9093 but nothing is paged
  # until a real receiver (Slack webhook, email, PagerDuty, etc.) is added here.
  - name: default
