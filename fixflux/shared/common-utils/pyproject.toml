[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "common-utils"
version = "0.1.0"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/common_utils"]

[tool.isort]
profile = "black"
known_first_party = ["common_utils"]
